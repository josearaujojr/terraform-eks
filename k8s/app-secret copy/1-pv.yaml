apiVersion: v1
kind: PersistentVolume
metadata:
  name: efs-pv
  namespace: app
spec:
  capacity:
    storage: 5Gi
  accessModes:
    - ReadWriteMany
  nfs:
    path: /  # dir no EFS onde os logs serão armazenados
    server: fs-0db256d6c46c6d9b1.efs.us-east-1.amazonaws.com
  storageClassName: efs-storage-class
